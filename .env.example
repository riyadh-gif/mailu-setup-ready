# Mailu Environment Configuration Template
# Copy this file to .env and configure with your values

###################################
# Common configuration variables
###################################

# Set this to the path where Mailu data and configuration is stored
ROOT=/srv/mailcow/mailu/data

# Mailu version to run (1.0, 1.1, etc. or master)
VERSION=master

# Set to a randomly generated 16 bytes string
# Generate with: openssl rand -hex 16
SECRET_KEY=CHANGE_THIS_TO_RANDOM_STRING

# Address where listening ports should bind
# Use 127.0.0.1 if you have Nginx reverse proxy
BIND_ADDRESS4=127.0.0.1
BIND_ADDRESS6=::1

# Subnet of the docker network
SUBNET=192.168.203.0/24

# Main mail domain
DOMAIN=your-domain.com

# Hostnames for this server, separated with commas
HOSTNAMES=mail.your-domain.com

# Postmaster local part (will append the main mail domain)
POSTMASTER=admin

# Choose how secure connections will behave
# Options: letsencrypt, cert, notls, mail, mail-letsencrypt
# Use 'notls' if external nginx handles SSL termination
TLS_FLAVOR=notls

# Authentication rate limit (per source IP address)
AUTH_RATELIMIT=10/minute

# Opt-out of statistics
DISABLE_STATISTICS=False

###################################
# Optional features
###################################

# Expose the admin interface (value: true, false)
ADMIN=true

# Choose which webmail to run (values: roundcube, snappymail, none)
WEBMAIL=roundcube

# Dav server implementation (value: radicale, none)
WEBDAV=none

# Antivirus solution (value: clamav, none)
ANTIVIRUS=none

###################################
# Mail settings
###################################

# Message size limit in bytes (default: 50MB)
MESSAGE_SIZE_LIMIT=50000000

# Message rate limit for outgoing messages (per user)
MESSAGE_RATELIMIT=100/day

# Networks granted relay permissions (comma separated)
# RELAYNETS=

# Disable DNSSEC validation check (if you have issues with DNS)
# RESOLVER_DNSSEC=false
